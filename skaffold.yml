apiVersion: skaffold/v2beta17
kind: Config
build:
  artifacts:
    - image: dockr.jfrog.io/mido-docker/api-gateway
      #jib: {}
      custom:
        buildCommand: mvn compile jib:build
  local: {}
deploy:
  kubectl:
    manifests:
      - k8s-*
